machine:
  node:
    version: 5.0.0

dependencies:
  override:
    - npm install

test:
  override:
    - npm test

deployment:
  codecov:
    branch: /.*/
    commands:
      - bash <(curl -s https://codecov.io/bash)
